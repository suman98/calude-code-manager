You can use the environment variable `export CLAUDE_CODE_OAUTH_TOKEN="token"` to switch between different Claude accounts.

Please choose one of the following options:

- Do not use `CLAUDE_CODE_OAUTH_TOKEN`
- Use `CLAUDE_CODE_OAUTH_TOKEN` (Token 1)
- Use `CLAUDE_CODE_OAUTH_TOKEN` (Token 2)
